import 'package:flutter/material.dart';

// Page backgrounds
const cream = Color(0xFFFAF3E7);  // oat milk — main page background
const foam = Color(0xFFFFFBF3);   // foam — section / surface background

// Text (ink)
const espresso = Color(0xFF2A1810);  // dark — headings
const cocoa = Color(0xFF6B4F3D);     // medium brown — body text

// Pastel cards (used on services & process)
const latte = Color(0xFFE8D5BC);    // warm tan
const matcha = Color(0xFFD5DDC0);   // soft sage
const caramel = Color(0xFFEDD9C2);  // light caramel
const peach = Color(0xFFEAC9B5);    // peach mousse
const vanilla = Color(0xFFF0E0CC);  // pale vanilla
